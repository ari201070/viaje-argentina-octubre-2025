# Handoff de continuidad - Viaje Argentina Octubre 2025

Fecha de handoff: 2026-07-30  
Repositorio local: `C:/Users/flier/GitHub/viaje-argentina-octubre-2025`  
Rama de trabajo: `develop`  
Estado Git al momento de este documento: `develop...origin/develop [ahead 2]`  
Ultimo commit local: `2ce4f0e4 data: replace photo catalog placeholder`

Este documento resume en detalle lo hecho hasta ahora para que el proyecto pueda retomarse en otra herramienta o por otro agente sin depender del historial de chat.

## Objetivo general del proyecto

Este repositorio es la nueva version de un proyecto anterior y documenta un viaje familiar realizado en Argentina en octubre de 2025.

La prioridad definida por el usuario es construir una plataforma Open Source con:

- React + Vite.
- OpenStreetMap / Leaflet.
- Soporte multidioma ES / HE / EN.
- PWA.
- Arquitectura modular.
- Datos separados del codigo de UI.
- Preservacion de informacion existente.
- Trabajo solo sobre la rama `develop`.
- Commits chicos, documentados y faciles de revisar.

## Restricciones importantes

- No eliminar informacion existente.
- No normalizar ni corregir textos con codificacion dudosa sin pedido explicito del usuario.
- Mantener la rama `develop` como unica rama de trabajo por ahora.
- Evitar meter al repo archivos multimedia masivos sin pipeline de optimizacion.
- Priorizar inventarios y referencias antes que copiar gigas de binarios.
- No mezclar datos crudos, assets publicos y logica React sin una razon clara.

## Documentacion revisada

Se hizo una primera lectura de la documentacion principal del repositorio antes de modificar archivos:

- `README.md`
- `PROJECT.md`
- `CHANGELOG.md`
- `docs/RULES_FOR_AI.md`
- `docs/GLOBAL_AI_SETUP.md`
- `docs/planning/ROADMAP.md`
- `docs/architecture/ARCHITECTURE.md`
- `docs/EXPLICACION_DEPLOY.md`
- `docs/EXPLICACION_SINCRONIZACION.md`

Conclusiones principales de esa lectura:

- La arquitectura objetivo esta documentada, pero la implementacion real todavia esta en una etapa inicial.
- El proyecto apunta a una app React/Vite con datos externos y componentes reutilizables.
- Hay intencion explicita de usar OpenStreetMap/Leaflet.
- Ya existe una base PWA, pero algunos archivos parecen heredados de un proyecto anterior.
- Existen archivos de idioma `es.json` y `he.json`, pero parecen arrastrados de otro proyecto llamado "Reverse Geocoding".
- Falta `en.json`.
- Hay mojibake en algunos textos acentuados y en hebreo, especialmente al visualizar desde PowerShell.
- La recomendacion vigente es no corregir esos textos automaticamente hasta decidir una estrategia de codificacion y contenido.

## Arquitectura observada

Archivos y carpetas relevantes detectados:

- `src/main.jsx`: punto de entrada React.
- `src/App.jsx`: app actual, todavia bastante concentrada en una landing/listado.
- `src/data/cities.json`: datos base de ciudades.
- `src/config/app.js`: configuracion de la app.
- `src/locales/es.json`: localizacion en espanol, heredada/parcial.
- `src/locales/he.json`: localizacion en hebreo, heredada/parcial.
- `src/index.css`: estilos globales.
- `public/manifest.json`: manifiesto PWA heredado/parcial.
- `public/sw.js`: service worker heredado/parcial.
- `legacy/`: zona para artefactos heredados.
- `docs/`: documentacion del proyecto.

Estado conceptual:

- El repo ya tiene React + Vite.
- Ya tiene dependencias base instaladas, incluyendo librerias para UI/mapa segun commits previos.
- La UI todavia no refleja toda la arquitectura modular deseada.
- PWA e i18n existen como piezas iniciales, pero requieren limpieza y adaptacion al producto real del viaje.

## Estado Git antes de los cambios de datos

Al comenzar el trabajo de datos:

- Rama: `develop`.
- Estado rastreado: limpio.
- `origin/develop` apuntaba a `5e71a47c build: install base libraries`.
- No habia `.git/index.lock`.

Commits previos relevantes vistos en historial:

- `5e71a47c build: install base libraries`
- `cf96ed76 feat: redesign landing page`
- `7c11345f docs: add architecture and roadmap`
- `cf81a967 feat: import useful assets from legacy repositories`

## Datos externos que paso el usuario

El usuario aporto estas fuentes:

- Album familiar completo: `F:/2025/Octubre/Viaje Familiar de 30 dias por Argentina`
- Documentos de viaje: `F:/Documentos_Viaje/2025/Octubre`
- Primer `photo_catalog.db`: `C:/Users/flier/GitHub/photo_catalog.db`
- Segundo `photo_catalog.db`, luego confirmado como correcto: `C:/Users/flier/.gemini/antigravity/scratch/photo_catalog.db`

## Revision de las fuentes externas

Se inspeccionaron las rutas externas sin mover todo de entrada.

Hallazgos:

- La carpeta `F:/Documentos_Viaje/2025/Octubre` contiene pocos documentos pequenos.
- La carpeta `F:/2025/Octubre/Viaje Familiar de 30 dias por Argentina` contiene un archivo multimedia familiar grande, de mas de 11 GB.
- El primer `photo_catalog.db` en `C:/Users/flier/GitHub/photo_catalog.db` pesaba `0` bytes, por lo que se trato como placeholder vacio.
- El segundo `photo_catalog.db` en `C:/Users/flier/.gemini/antigravity/scratch/photo_catalog.db` pesa `37,560,320` bytes y tiene encabezado `SQLite format 3`.

Decision tomada:

- Copiar al repo solo documentos y referencias pequenas.
- No copiar el album multimedia completo de mas de 11 GB.
- Registrar el album completo como fuente externa inventariada en JSON.
- Guardar el `photo_catalog.db` correcto en `legacy/photo_catalog.db`.

## Archivos agregados o modificados

### Documentos importados

Destino: `public/travel-documents/`

Archivos importados desde `F:/Documentos_Viaje/2025/Octubre`:

- `Booking_Argentina_2025_Hoteles.png`
- `Confirmacion_Fuente Mayor Hotel Centro.pdf` o variante con mojibake en nombre segun filesystem/consola.
- `itinerary_Y0HBMB59-1.pdf`
- `Screenshot_20251011_142528_Bookingcom.jpg`
- `Screenshot_20251018_181403_Bookingcom.jpg`
- `ticket_NTTZSH47.pdf`

Nota: en algunos outputs de PowerShell el nombre `Confirmacion` aparecio con mojibake como `Confirmaciֳ³n`. No se corrigio automaticamente para evitar perdida o renombrado accidental.

### Imagenes y capturas de referencia

Destino: `public/travel-references/`

Archivos importados desde la raiz del album familiar:

- `IMG-20251025-WA0014.jpg`
- `Screenshot_20251014_141216_Chrome.jpg`
- `Screenshot_20251014_151450_Chrome.jpg`
- `Screenshot_20251014_151643_Xe.jpg`
- `Screenshot_20251014_151959_Xe.jpg`
- `Screenshot_20251028_112431_Maps.jpg`

### Base de datos heredada

Destino: `legacy/photo_catalog.db`

Historia:

- Primero se copio un placeholder de `C:/Users/flier/GitHub/photo_catalog.db`.
- Ese archivo pesaba `0` bytes.
- Luego el usuario paso la DB correcta desde `C:/Users/flier/.gemini/antigravity/scratch/photo_catalog.db`.
- Se reemplazo el placeholder por la DB correcta.

Metadatos de la DB correcta:

- Ruta original: `C:/Users/flier/.gemini/antigravity/scratch/photo_catalog.db`
- Ruta en repo: `legacy/photo_catalog.db`
- Tamano: `37,560,320` bytes
- Formato: `SQLite format 3`
- SHA256: `F968B23D37A9F59DD2CF7FD7CC430A65F53C08E55DBBB27A5EF5D4B9F4099BD2`
- Last modified registrado: `2026-07-24T12:46:16+03:00`

### Inventario estructurado

Archivo: `src/data/sources/travelSources.json`

Contenido conceptual:

- `generatedAt`: `2026-07-30`
- `purpose`: inventario de fuentes externas y assets pequenos importados.
- `imported.documents`: documentos copiados a `public/travel-documents/`.
- `imported.references`: capturas e imagenes copiadas a `public/travel-references/`.
- `imported.legacyDatabase`: metadata del `photo_catalog.db` correcto.
- `externalSources.familyAlbum`: inventario del album grande que no se copio a Git.
- `externalSources.travelDocuments`: resumen de la fuente de documentos.

Nota tecnica:

- El JSON se valido con Node.
- En un primer intento PowerShell lo escribio con BOM.
- Luego se reescribio sin BOM para evitar problemas en imports de Vite/Node.
- Despues de reemplazar la DB se evito reformatear todo el JSON para que el diff quedara revisable.

### Nota documental de datos

Archivo: `docs/data-sources/README.md`

Contenido:

- Explica que se importo al repo.
- Explica que el album completo queda referenciado externamente.
- Indica ruta original del album.
- Indica que futuras pipelines deben generar derivados optimizados en vez de commitear originales.
- Fue actualizado para reflejar que `legacy/photo_catalog.db` ya no es placeholder vacio sino SQLite real.

## Commits creados

### `69890542 data: import travel source inventory`

Primer commit de datos.

Incluyo:

- Creacion de `docs/data-sources/README.md`.
- Creacion de `src/data/sources/travelSources.json`.
- Copia de documentos pequenos a `public/travel-documents/`.
- Copia de imagenes/capturas de referencia a `public/travel-references/`.
- Copia inicial de `legacy/photo_catalog.db`, en ese momento placeholder vacio.

Validaciones asociadas:

- `travelSources.json` parseaba correctamente.
- `npm run build` OK.
- `npm run lint` OK.

Nota:

- `npm run build` y `npm run lint` fallaron inicialmente dentro del sandbox por permisos al resolver `C:/Users/flier`.
- Luego se reintentaron con permisos elevados y pasaron correctamente.

### `2ce4f0e4 data: replace photo catalog placeholder`

Segundo commit de datos.

Incluyo:

- Reemplazo de `legacy/photo_catalog.db` por la DB correcta.
- Actualizacion del bloque `imported.legacyDatabase` en `src/data/sources/travelSources.json`.
- Actualizacion de `docs/data-sources/README.md`.

Validaciones asociadas:

- Encabezado binario confirmado: `SQLite format 3`.
- SHA256 confirmado: `F968B23D37A9F59DD2CF7FD7CC430A65F53C08E55DBBB27A5EF5D4B9F4099BD2`.
- `travelSources.json` parsea correctamente.
- Estado Git final limpio.

## Estado actual del repo

Al momento de escribir este handoff:

- Rama: `develop`
- Estado: limpio antes de crear este archivo de handoff.
- Relacion remota: `develop` esta `ahead 2` respecto de `origin/develop`.
- No hay locks de Git detectados.
- Commits locales no pusheados:
  - `69890542 data: import travel source inventory`
  - `2ce4f0e4 data: replace photo catalog placeholder`

Despues de agregar este archivo, el arbol tendra un cambio no commiteado:

- `docs/HANDOFF_2026-07-30.md`

Este archivo todavia debe validarse, stagearse y commitearse si se quiere preservar en Git.

## Chequeo de procesos y estabilidad

El usuario reporto que la sesion se cerro varias veces.

Se hizo un diagnostico de procesos antes de seguir:

- No se observo saturacion fuerte de CPU en muestra instantanea.
- `chrome` tenia 13 procesos y aproximadamente `1.43 GB` de RAM.
- `Dropbox` tenia aproximadamente `725 MB` de RAM y algo de actividad.
- Procesos `Codex/codex` sumaban aproximadamente `978 MB`.
- `node_repl` tenia 5 procesos pero solo aproximadamente `35 MB` en total.
- Procesos `git`, `git-bash` y `bash` consumian muy poco.
- No habia `.git/index.lock`.

Recomendacion operativa:

- No matar procesos `Codex/codex` desde herramientas automaticas porque podria cerrar la sesion actual.
- Cerrar manualmente pestañas o ventanas de Chrome innecesarias.
- Pausar/cerrar Dropbox temporalmente si vuelve a haber cierres.
- Dejar abierta la terminal `MINGW64 ~/GitHub/viaje-argentina-octubre-2025 (develop)` si se esta usando, pero si solo espera en `$`, no es critica.

## Problemas conocidos o pendientes

### Codificacion / mojibake

Hay mojibake visible en algunos documentos y salidas de consola:

- Textos acentuados en espanol.
- Nombres o carpetas en hebreo.
- Algunos nombres del JSON generados desde rutas de Windows.

No se corrigio por ahora.

Siguiente paso recomendado:

- Definir estrategia de codificacion antes de renombrar o normalizar.
- Confirmar si el problema esta en los archivos reales, en PowerShell, o solo en renderizado de consola.
- Usar herramientas que lean UTF-8 correctamente antes de modificar nombres.

### PWA heredada

`public/manifest.json` y `public/sw.js` parecen venir de un proyecto anterior.

Riesgo:

- Pueden referenciar rutas, nombres o assets que no corresponden al proyecto actual.

Siguiente paso recomendado:

- Auditar `manifest.json` y `sw.js`.
- Adaptarlos al viaje familiar.
- Verificar que Vite sirva correctamente los assets.
- Definir iconos y cache strategy real para la app.

### i18n incompleto

Existen:

- `src/locales/es.json`
- `src/locales/he.json`

Falta:

- `src/locales/en.json`

Riesgo:

- Los textos actuales parecen heredados de "Reverse Geocoding".
- La app todavia no parece tener una capa i18n completa y conectada.

Siguiente paso recomendado:

- Crear una estructura i18n clara.
- Migrar textos actuales de UI a claves.
- Agregar `en.json`.
- Revisar direccion RTL para hebreo.

### Arquitectura modular pendiente

`src/App.jsx` todavia concentra demasiado.

Siguiente paso recomendado:

- Separar layout, data views, mapa, selector de idioma y componentes de ciudad.
- Mantener `src/data/` como fuente de datos.
- Evitar introducir abstracciones grandes hasta tener una primera pantalla funcional.

### Album multimedia grande

El album completo supera los 11 GB.

Decision vigente:

- No versionarlo completo.
- Mantenerlo como fuente externa inventariada.

Siguiente paso recomendado:

- Crear pipeline de ingesta de media.
- Generar derivados optimizados para web.
- Crear thumbnails y metadata.
- Definir si los derivados van a `public/media/`, storage externo o releases.

## Archivos clave para retomar

Para entender lo hecho:

- `docs/HANDOFF_2026-07-30.md`
- `docs/data-sources/README.md`
- `src/data/sources/travelSources.json`
- `legacy/photo_catalog.db`

Para entender la app:

- `README.md`
- `PROJECT.md`
- `docs/architecture/ARCHITECTURE.md`
- `docs/planning/ROADMAP.md`
- `src/App.jsx`
- `src/data/cities.json`
- `src/config/app.js`
- `src/locales/es.json`
- `src/locales/he.json`
- `public/manifest.json`
- `public/sw.js`

## Comandos utiles para retomar

Ver estado:

```bash
git status --short --branch
```

Ver commits locales:

```bash
git log --oneline --decorate -6
```

Validar JSON:

```bash
node -e "JSON.parse(require('fs').readFileSync('src/data/sources/travelSources.json','utf8')); console.log('travelSources.json OK')"
```

Ver hash de la DB:

```powershell
Get-FileHash -LiteralPath 'legacy\photo_catalog.db' -Algorithm SHA256
```

Build:

```bash
npm run build
```

Lint:

```bash
npm run lint
```

Nota:

- Si `npm run build` o `npm run lint` fallan por permisos del sandbox y no por codigo, reintentarlos en entorno con permisos suficientes.

## Proxima ruta sugerida

Orden recomendado para continuar:

1. Confirmar que el archivo de handoff queda commiteado o copiado fuera del repo.
2. Pushear `develop` si se quiere respaldar los dos commits locales.
3. Auditar y corregir PWA heredada (`manifest.json`, `sw.js`) para que represente el viaje.
4. Crear `src/locales/en.json` y definir un mecanismo i18n minimo.
5. Separar `src/App.jsx` en componentes.
6. Preparar una primera integracion visual de `travelSources.json` en la UI.
7. Planear pipeline de media para el album de mas de 11 GB.
8. Revisar el contenido de `photo_catalog.db` con SQLite cuando haya herramienta disponible.

## Mensaje para el siguiente agente/herramienta

Este repo ya fue inspeccionado y se respeto la rama `develop`. No empezar borrando ni "limpiando" archivos heredados. Hay informacion antigua que puede ser util. La prioridad inmediata no es hacer una gran refactorizacion, sino estabilizar las bases: PWA correcta, i18n real, datos bien modelados, y una UI modular que pueda crecer.

Los dos commits locales de datos son intencionales y deben preservarse:

- `69890542 data: import travel source inventory`
- `2ce4f0e4 data: replace photo catalog placeholder`

El archivo `legacy/photo_catalog.db` es grande pero fue incorporado deliberadamente porque contiene el catalogo real de fotos. El album completo de mas de 11 GB no esta en Git y no debe copiarse completo sin una decision explicita.

Si se necesita avanzar con rapidez, el mejor siguiente cambio chico y revisable probablemente sea arreglar la PWA heredada o agregar la base i18n `en.json` sin tocar todavia el archivo multimedia grande.
